<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8FAFC">

    <LinearLayout
        android:id="@+id/headerCal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="24dp"
        android:paddingTop="20dp"
        android:paddingBottom="10dp"
        android:background="#FFFFFF">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Календар"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#1E293B"/>
    </LinearLayout>

    <CalendarView
        android:id="@+id/calendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/headerCal"
        android:background="#FFFFFF"
        android:theme="@style/Theme.MaterialComponents.Light"
        android:focusedMonthDateColor="#4F46E5"/>

    <TextView
        android:id="@+id/tvTaskCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/calendarView"
        android:padding="8dp"
        android:gravity="center"
        android:textColor="#4F46E5"
        android:textStyle="italic"
        android:text="Избери дата, за да видиш задачите"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCalendarTasks"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/tvTaskCount"
        android:layout_above="@id/bottomNavigationCard"
        android:padding="12dp"
        android:clipToPadding="false"/>

    <androidx.cardview.widget.CardView
        android:id="@+id/bottomNavigationCard"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="6dp">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center">
            <Button android:id="@+id/btnNavHome" android:layout_width="0dp" android:layout_height="match_parent" android:layout_weight="1" android:text="Задачи" android:textColor="#64748B" style="?android:attr/borderlessButtonStyle"/>
            <Button android:id="@+id/btnNavAdd" android:layout_width="0dp" android:layout_height="match_parent" android:layout_weight="1" android:text="+ Нова" android:textColor="#64748B" style="?android:attr/borderlessButtonStyle"/>
            <Button android:id="@+id/btnNavCalendar" android:layout_width="0dp" android:layout_height="match_parent" android:layout_weight="1" android:text="Календар" android:textColor="#4F46E5" android:textStyle="bold" style="?android:attr/borderlessButtonStyle"/>
            <Button android:id="@+id/btnNavStats" android:layout_width="0dp" android:layout_height="match_parent" android:layout_weight="1" android:text="Прогрес" android:textColor="#64748B" style="?android:attr/borderlessButtonStyle"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</RelativeLayout>